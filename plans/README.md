# Animation plans

Generated by `improve-animations` from the `find-animation-opportunities` sweep on commit `ae63021` (001, 002) and a second-wave sweep on commit `d583fec` (003).

| # | Title | Severity | Status |
| --- | --- | --- | --- |
| [001](001-scroll-triggered-section-reveals.md) | Make hero-below homepage sections reveal on scroll | MEDIUM | DONE |
| [002](002-press-feedback-active-states.md) | Add press feedback (`:active`) to primary buttons and clickable cards | LOW | DONE |
| [003](003-mobile-nav-panel-transition.md) | Animate mobile navigation panel open/close | MEDIUM | DONE |

001 and 002 landed together in commit `49cf74f`. 003 required a mid-implementation fix — see its Postmortem section: the planned `display: none ↔ block` + `@starting-style` approach didn't animate the open direction in real-browser testing (Chrome 152), so the discrete property was swapped to `visibility` instead.

## Recommended execution order

**003 is independent of 001/002** and can run any time — it's CSS-only, touches a single unrelated selector (`.inc-nav`), and doesn't share any rule with either landed plan.

## Dependencies

None. 003 touches `src/app/globals.css` only inside the existing `@media (max-width: 900px)` block (lines 535-552) and the existing `@media (prefers-reduced-motion: reduce)` block (lines 2657-2669) — disjoint from where 001/002 made their edits.
